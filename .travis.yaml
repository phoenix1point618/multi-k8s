sudo: required
services:
  - docker

before_install:
  - curl https://sdk.cloud.google.com | bash > /dev/null;
  - sources $HOME/google-cloud-sdk/path.bash.inc
  - gcloud components update kubectl
  - gloud auth activate-service-account --key-file activate-service-account.json